# MedChron backend deps (installed into the same D: venv as the ML core).
# The ML core (medchron, torch, opencv, numpy) is installed separately via ml/pyproject.toml.
fastapi>=0.110
uvicorn[standard]>=0.29
sqlmodel>=0.0.16
python-multipart>=0.0.9
pydantic>=2.6
# Postgres driver (for a Supabase DATABASE_URL in production; unused with SQLite)
psycopg2-binary>=2.9
reportlab>=5.0
